# M0.3 合并上游并执行集成回归

- 状态：review
- 日期：2026-08-06
- 上游：`v1.0.1` / `505877cc5160d3ea5cdb5bbd72902db03c97dd10`
- 分支：`integration/upstream-v1.0.1`

## 合并与冲突

- 使用 `--no-commit --no-ff --allow-unrelated-histories` 合并精确 SHA。
- 唯一内容冲突为根 `README.md`；解决结果以 PersonalMemory 产品定位为首页，并保留上游 `README_CN.md`、许可证和完整源码。
- 未引入 `feat/server_team` 或其他 v2 分支头。

## 安全基线调整

- 清除 `scripts/start-offload-local.sh` 中疑似真实的默认 LLM 凭据；环境变量缺省值改为空。
- 本地启动脚本默认只监听 `127.0.0.1`，模型地址、模型名称和 Opik 地址默认为空，Opik 默认关闭。
- 移除安装依赖时静默修改 OpenClaw 的 `postinstall`，改为显式 `npm run patch:openclaw`。
- 从 npm 官方 registry 重新生成并提交根项目和 TypeScript SDK 的 `package-lock.json`；锁文件不固化第三方镜像地址，后续使用确定性安装。
- 升级 AI SDK、OpenTelemetry 和 TypeScript SDK 测试工具链到无已知 high/critical 的解析版本。
- 暂时裁剪可选 Opik 依赖：其当前发布包仍传递引入已知 high 的旧 AI SDK/Undici 链。Opik 代码路径继续采用动态加载和无依赖降级；恢复该能力前必须重新通过安全审计与功能测试。

## 最小回归保护

- 新增文本检索测试、安全默认值测试，以及隔离本地存储读写与路径越界保护的核心 smoke。
- 使用仅监听 loopback 的临时 OpenAI-compatible HTTP server，执行 AI SDK 与 provider 的真实请求/响应链；实际初始化并关闭升级后的 OpenTelemetry SDK；验证未安装 Opik 时安全降级且不外联。
- 真实 provider 测试发现并修复 AI SDK 7 token usage 字段由 `prompt/completion` 迁移为 `input/output` 后统计归零的问题；实现同时兼容新旧字段。
- 根 `npm test`：4 个文件、10 项通过。
- 根 `npm run build`：通过。
- TypeScript SDK build：通过；2 个文件、32 项测试通过。
- Python SDK：34 项单元测试通过；测试缓存因沙箱目录权限产生 1 条非功能警告。
- 根项目与 TypeScript SDK 使用 npm 官方 registry 执行 audit：均为 0 vulnerabilities。

## 待审查

- README 冲突解决是否完整保留归属与产品边界；
- 依赖大版本升级是否存在构建未覆盖的运行时 API 变化；
- Opik 裁剪是否被正确记录且不会影响默认本地模式；
- 锁文件、秘密扫描和 Git 双祖先关系是否满足提交门禁。
