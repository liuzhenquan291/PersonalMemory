# M0.6 上游同步 Runbook 与只读演练

- 状态：completed
- 日期：2026-08-07
- 产品 HEAD：`3259cc2f7dc219863c1af030c6bb694660d30c8e`
- 基线：`v1.0.1` / `505877cc5160d3ea5cdb5bbd72902db03c97dd10`
- 演练候选：`upstream/feat/server` / `e57b8cb768fe685afebdb9f4494f625cab092eda`

## 1. 演练范围和安全保证

本次演练 Git 操作更新 `refs/remotes/upstream/*`、读取 Git 对象，并查询远端 heads/tags；没有 checkout、merge、cherry-pick、rebase、上游代码工作树写入、`main` 移动或 push。本步骤仅新增评估文档。第一次按用户提供的 `feat/server-team` 查询失败后，通过 `git ls-remote --heads upstream` 确认实际分支名是 `feat/server_team`。远端 `v1.0.1` 是轻量 tag，SHA 为已知 release commit `505877c`，与本地 `v1.0.1^{commit}` 一致。

## 2. 三条代码线拓扑

| 代码线 | SHA | 与 `v1.0.1` 的 merge-base | 结论 |
|---|---|---|---|
| `feat/server` | `e57b8cb` | `505877c` | 基线是其祖先；0 behind / 5 ahead，可做增量评估 |
| `feat/server_team` | `fe3230f` | 无 | 不相关历史；禁止直接 merge |
| `main` | `3c6fc64` | 无 | 不相关历史；禁止直接 merge |

`main` 与 `feat/server_team` 的对称差异计数分别为 12/105 和 12/8，但没有共同祖先，计数不能被解读为可线性同步。

## 3. `v1.0.1..feat/server` 提交分类

| SHA | 上游标题 | 分类 | 决定 | 理由/复议条件 |
|---|---|---|---|---|
| `c81c70b` | Update README.md (#536) | 文档 | defer | 上游安装描述不直接构成 PersonalMemory 用户文档；M5 编写安装文档时按事实选择性参考 |
| `6e55141` | fix(gateway): route local L2 timers to scene extraction | 修复证据/测试 | selective | 该提交实际只新增 timer routing 测试，未改变本区间产品代码；M1.1 建立统一 verify 时评估移植测试 |
| `e714dfa` | feat(gateway): add Offload V2 result ref recovery (#577) | 功能 + API/SDK 漂移 | defer | 新增 `/v2/offload/read-ref`、两套 SDK 方法和有界读取；Offload 不属于 Chat Memory MVP。它与本地 `MemoryClient.close()` 改动重叠，整体覆盖或错误解冲突可能回退连接池释放；未来采用时必须保留 `http.ts` 的 TLS/defaultRuntime/close 和 `MemoryClient.close()` 并重跑相关测试，再独立评审 session ownership、返回预算和敏感工具结果读取 |
| `86d942a` | Update README_CN.md (#617) | 文档 | defer | 与产品代码无关；不单独同步 |
| `e57b8cb` | Update README_CN.md (#803) | 文档 | defer | 与产品代码无关；不单独同步 |

本轮结论：`accept = 0`、`selective = 1`、`defer = 4`、`reject = 0`。不合入候选上游提交，继续以 `v1.0.1` 为产品代码基线。

## 4. 风险清单

- 差异规模：22 个文件，新增 826 行、删除 13 行。
- 依赖/锁文件：无变化。
- LICENSE/第三方声明：无变化。
- 安装脚本、Docker/Compose、CI：无变化。
- SQLite/schema/migration：无变化。
- 数据格式：未迁移现有 schema；沿用已存在的 `refs/<tool_call_id>.md`，新增受限读取 API，并改变压缩消息中恢复提示。
- API/SDK：新增 Offload read-ref；Python 与 TypeScript SDK 同步扩展。
- 冲突面：上游触及 `sdk/typescript/src/client.ts` 和 `src/gateway/server.ts`；前者与本地 `MemoryClient.close()` 重叠，后者邻近本地 Gateway 生命周期改动。同步时必须保留 `http.ts` TLS/defaultRuntime/close、客户端幂等关闭和 Gateway 安全关闭测试，不能整体覆盖或草率解冲突。
- 安全：handler 校验引用属于 session 的直接 `.md` 子项并限制 token，但 archived tool result 本身可能高度敏感；未来采用前仍需纳入权限、审计、删除和导出边界。

## 5. 发布前建议

当前不需要同步。5 个上游提交没有修复 PersonalMemory MVP 的阻断问题；唯一功能属于延期范围，整体合并反而会扩大 SDK 冲突和安全回归面。下一次评估触发条件：

1. M1.1 统一 verify 时重新评估 `6e55141` 测试；
2. 产品决定开放 Offload 或工具结果恢复时重新评估 `e714dfa`；
3. 发布候选冻结前按 Runbook 比较 `e57b8cb..upstream/feat/server`，同时携带并复审本轮 selective/defer ledger；
4. 上游出现 security、数据损坏、兼容或依赖公告时立即评估，不等待发布窗口。

## 6. 检查与审查门

- [x] 固定三个远端 SHA、基线和产品 HEAD。
- [x] 证明只有 `feat/server` 与基线同历史。
- [x] 分类修复、功能、架构/API 漂移、依赖、数据格式和文档。
- [x] 显式检查依赖、许可证、安装脚本、schema 和产品冲突面。
- [x] 所有不同步提交均有理由和复议条件。
- [x] 未实际合入任何未经确认的上游代码。
- [x] 独立审查 P0/P1/P2 清零后标记 `completed`。

## 7. 独立审查结论

- 初审发现 tag/连续基线语义、选择性移植账本、冲突恢复和 `e714dfa` 冲突描述问题，未提交。
- 复审进一步校正 fetch 的审计边界、远端 tag 防篡改检查和发布前延期账本。
- 最终结论：P0 = 0、P1 = 0、P2 = 0；P3 的首次 ref `absent` 表述已处理，可以提交。

## 8. M0 退出门复验

- 根项目 build 通过；常规测试 4 个文件、10 项通过。
- standalone 黄金链路通过，测试主体耗时约 4.2 秒。
- TypeScript SDK build 通过，3 个文件、35 项测试通过。
- Python SDK 34 项测试通过。
- 根项目和 TypeScript SDK 的官方 npm audit 均为 0 漏洞。
- M0.0–M0.6 实施记录均为 `completed`，四项 ADR、五个端到端场景、威胁模型、供应链/性能基线和同步 Runbook 已记录；独立审查无未解决 P0/P1/P2。
