# M3.2 冲突、合并和替代关系

- 状态：completed
- 日期：2026-08-12
- 基线：`254f955`

## 交付范围

- 增加版本化有效期表和冲突/替代关系表，保留活动与已撤销关系历史。
- 增加人工冲突、人工合并、替代撤销及有效期 API；拒绝自替代和传递循环。
- 统一召回过滤未生效、已过期、被替代及用户已确认冲突的记忆；冲突关系暂停双方但不自动选择事实。
- 记忆详情页展示召回状态、有效期和关系，并提供相似候选搜索、人工判断、合并与撤销。
- JSON/Markdown 可读导出增加有效期和关系记录。

## 检查与审查门

- [x] Core 63 项覆盖互相矛盾、有效期、循环替代、关系历史和合并撤销。
- [x] Gateway 73 项覆盖显式人工关系、上游更新失败、循环校验先于正文更新和召回过滤。
- [x] Web 12 项通过；Playwright 5 项覆盖完整人工路径和既有窄屏门禁。
- [x] 黄金路径覆盖替代抑制、撤销恢复和可读导出。

## 审查修正

- 迁移按框架要求拆成单条无分号语句，避免批量 SQL 绕过迁移校验。
- 合并流程先校验重复关系和完整替代图，再更新上游正文，避免循环关系失败后正文已变化。
- 活动关系的相同原因视为幂等重试，不同原因返回冲突，避免静默覆盖用户判断依据。
- 合并正文以 SHA-256 区分相同重试和不同修改；只保存摘要，不在治理表复制正文。

## 已知边界

- 相似候选复用有界 L1 搜索，最多展示第一页结果，不声明自动冲突检测能力。
- M3.2 不删除被替代记忆；可验证级联删除属于 M3.4。
- 全局事件审计和更丰富的关系时间线界面属于 M3.3。
