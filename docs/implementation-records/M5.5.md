# M5.5 真实分发物重新发布验证

- 状态：in progress（不得宣称 MVP / 发布候选已通过）
- 执行日期：2026-08-26
- 固定起点：`6de5c48`
- 候选形式：`PersonalMemory-0.1.1-source.tar.gz` + 独立 SHA-256

## 已完成证据

- 发布扫描通过：MIT、20 个原生平台锁包、3 类秘密模式和 568 个候选源码文件；两次独立打包摘要一致。
- npm 官方 registry 审计发现 `nanoid <3.3.18` high 与 `smol-toml <1.6.1` moderate；锁文件升级后官方审计为 0 漏洞。默认 npmmirror 的 audit endpoint 返回 404，不计为审计结果。
- 真实 Codex CLI `0.149.1` 在隔离 HOME 和本地 Gateway fixture 下完成五个 PersonalMemory MCP 工具的真实模型会话。
- macOS 14.5 arm64、Node.js 22.22.1、npm 10.9.4 从校验后的真实源码包完成全新依赖安装（0 漏洞）、构建、首次安装、非降级召回、重复安装和脱敏状态检查。
- macOS 状态证明四个受管进程、Codex `installed_untrusted`、Claude Hook `installed`、健康 worker、空 backlog、默认双授权关闭和保留期执行关闭。
- macOS 真实包完成 3 项资产备份、校验、恢复、自动重启、恢复后健康检查、默认保留数据卸载与 8420/8787/4173 端口清理。
- 真实 Codex 0.149.1 经 `/hooks` 逐项核对并信任候选包精确定义后，自然提示实际触发 `UserPromptSubmit` 与 `Stop`；一轮成功回答生成 1 份幂等账本和 2 条 L0 消息。
- 临时隔离安装的真实 Claude Code 2.1.228 完成五工具 MCP E2E；读取真实分发物安装的受管配置后，自然会话明确报告两个 Hook 成功，并生成对应事件回执、1 份账本和 2 条 L0 消息。
- 使用已审批 L1 验收种子时，本地模型替身确认 Claude 的模型请求在处理用户提示前已包含该记忆，随后 `Stop` 自动捕获成功；该证据区分了“Hook 被调用”和“召回内容实际注入”。
- 真实包完成 Hook 授权启用/撤销即时生效、模型授权 `required → authorized → revoked` 及每次重启后的持久状态验证。
- 重复 `Stop` 只生成一份账本；明确凭据模式触发整轮敏感内容阻断；继承 HTTP/HTTPS/ALL proxy 时本地监听器记录 0 次连接。
- Gateway 故障时捕获进入私有 outbox；重启后队列清空并只新增一份账本和两条 L0。50 轮连续 Hook 运行生成 50 份账本和 100 条 L0，worker 保持健康、backlog 为 0。
- 从首个发布候选提交 `c1c909c` 重新生成并校验旧分发物，完成 v2 回执安装、3 项资产升级前备份校验、迁移、v3 四进程重启和重复升级幂等验证。

## 真实分发物发现并修复

1. 发布秘密扫描会把测试中的完整私钥头当作候选泄漏；测试向量改为运行时拼接，敏感识别断言保持有效。
2. 干净包在 `npm ci` 后直接导入尚未构建的 workspace `dist`；安装入口现先执行产品构建，发布扫描固定检查该步骤。
3. 上游 Gateway 会在产品 Gateway 安全检查前以非私有权限创建数据目录；安装器现先复用 core 的 `initializeDataDirectory` 完成符号链接、祖先所有权和 `0700` 校验，再启动任何服务。
4. 产品 Gateway 未收到 M4.6.6 后要求的 `PERSONALMEMORY_STATE_DIR`；安装器现显式传递受管状态目录。
5. worker 与 Gateway 同时启动时会在 Gateway 就绪前首次同步失败，30 秒安装窗口内无法恢复；worker 现只在三个健康入口与非降级召回通过后启动。

## 尚未完成的验收项与阻断

- Linux arm64：真实包 SHA-256 已在 `node:22.22.1-bookworm-slim` 校验通过，但依赖阶段后 Docker Desktop 报 `containerd ... meta.db: input/output error`；随后最小容器、状态查询和引擎重启均无法正常完成。产品服务尚未启动，该结果是宿主 Docker 环境阻断，不是 Linux 产品通过或失败。
- 2026-08-26 再次检查时 Docker Server 仍在 8 秒内无响应，本机也没有 Colima、Lima、Podman、Finch 或 nerdctl 替代运行时。重置 Docker Desktop 存储可能删除本机镜像、容器和卷，未经用户明确授权不执行。
- 因 Linux 产品服务尚未完成真实验收，本步骤不更新为 completed，不重新授予“完整 MVP / 发布候选”状态，不移动 `main`、不 push、不发布。

## 后续复验入口

1. 经用户确认可处理 Docker Desktop 本地数据后，修复或重置其存储；从本记录候选源重新生成分发物，在 Linux arm64 重跑校验、首次/重复安装、自然 Hook、状态、升级、备份恢复、故障恢复、卸载与端口清理。
2. Linux 通过后重跑 `npm run verify`、发布扫描、官方 npm audit 和 Standards/Spec 独立复审，再决定是否把 M5.5 标记 completed。
