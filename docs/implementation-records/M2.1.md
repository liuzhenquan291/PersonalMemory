# M2.1 固化测试数据和验收场景

- 状态：completed
- 日期：2026-08-10
- 基线：`b124619`

## 交付范围

- 建立版本化 `m2.1-v1` 合成黄金数据集，共 100 个样本。
- 覆盖偏好、事实、决定、冲突、过期和无答案六类，以及中英文输入。
- 固定 12 样本 smoke 子集，覆盖全部类别和语言。
- 明确 E2E-01–E2E-05 的 fixture 复用边界和后续自动化步骤。
- 增加结构、确定性、来源、时效与答案泄漏防护测试。

## 非目标

- 不在 M2.1 实现会话导入、提炼、检索或模型评测执行器。
- 不把数据结构测试表述为召回质量已经通过。
- 不使用真实用户会话或真实个人信息。

## 检查与审查门

- [x] 核心包 38 项测试通过，其中 4 项覆盖黄金数据集契约。
- [x] 仓库完整 verify 通过，包括构建、类型、lint、边界、产品测试、standalone 黄金链路和 TypeScript SDK 35 项测试。
- [x] Python SDK 34 项测试通过；沙箱禁止写 pytest 缓存产生 1 条非功能警告。
- [x] 根项目与 TypeScript SDK 使用 npm 官方 registry 审计，均为 0 漏洞。
- [x] `git diff --check` 通过且无无关变更。
- [x] 独立审查 P0/P1/P2 清零后标记 `completed`。

## 审查结论

首轮审查发现 1 项 P1：E2E-02 明确要求中英文多轮会话，但初稿多数样本只有单条用户消息。
提交前已将全部样本改为至少两轮且同时含 user/assistant，并增加自动约束；冲突和过期检查同步按语义定位来源消息，完整门禁复测通过。

最终结论：P0=0、P1=0、P2=0。M2.1 完成，可以进入 M2.2。
