# M3.1 记忆收件箱

- 状态：completed
- 日期：2026-08-12
- 基线：`1d1482e`

## 交付范围

- 增加版本化审核表及 `pending/approved/rejected` 状态机，未见过的 L1 默认待审核。
- 增加单项/批量接受、修改后接受和带原因拒绝 API；支持乐观并发、重复提交和逐项失败结果。
- 统一召回在读取 L1 后强制审核过滤，未批准和已拒绝记忆不能注入上下文。
- 增加 Web 收件箱、批量选择、内容校正、接受/拒绝操作及 320px 窄屏适配。
- 可读导出增加审核状态，完整备份继续通过产品 SQLite 保留审核记录。

## 检查与审查门

- [x] Core 59、Gateway 64、Web 11 项专项测试通过，四个产品包类型检查通过。
- [x] Playwright 4 项通过，覆盖键盘路径、320px 横向溢出、收件箱直接接受和减少动画。
- [x] 黄金路径验证待审核不可召回、接受后可召回、重启/导出保留审核状态。
- [x] 批量部分失败、同 ID 串行、重复提交、版本冲突和上游更新失败已有自动化覆盖。

## 审查修正

- 审核过滤改为先拉取有界候选、按状态过滤后分页，避免在上游分页后过滤造成漏项。
- `review_status` 仅允许用于 L1，避免界面暗示尚未实现的 L2/L3 审核能力。
- 修改后接受采用上游成功优先、审核落账随后执行的顺序，避免正文更新失败但记忆已获批准。
- 浏览器验收发现并修复“未编辑直接接受”访问空草稿的问题，以及新增导航后 320px 的 1px 横向溢出。

## 已知边界

- 收件箱首版固定读取第一页 12 条，分页交互后续补充。
- M3.1 不提供相似候选合并、替代关系、事件审计或级联删除，分别由 M3.2–M3.4 交付。
