# M4.5.5 双客户端受管 Hook 安装与维护 worker

- 状态：completed
- 日期：2026-08-20
- 基线：69c6170

## 边界

- 私有回执管理 Codex hooks.json 与 Claude Code settings.json；保留无关设置，冲突、重复、编辑、回执损坏均 fail closed。
- 命令只含 Node、受管入口、客户端名、非敏感的绝对状态目录定位参数和当前定义标识。Bearer、HMAC 私钥、正文和 cwd 不进入配置、回执或状态。
- Codex 初装/升级报告 installed_untrusted；当前精确定义产生首事件后才 healthy。升级清除旧回执，不绕过信任。
- 安装回执版本 3 管理四个进程。安装器等待真实 worker PID 首次报健康；worker 按固定 60 秒节拍限量清扫过期 turn、维护 outbox，并写入私有脱敏状态。
- lifecycle status 合并配置、信任/首事件、最近维护、授权 revision 和 backlog。卸载只移除回执证明未修改的条目。
- 隔离 HOME fixture 执行双客户端自然提示与 Stop；真实 Codex doctor 解析配置。当前环境无 Claude Code 二进制，留待 M5.5 分发矩阵复核。
- M4.6 前生产 recall/capture 授权与 capture sink 继续关闭；fixture 不改变该门禁。

## 验证与审查

- [x] 测试先行：先观察安装模块和定时清扫接口缺失。
- [x] 8 项安装、10 项生命周期、13 项 Adapter、15 项 Runtime/outbox 与 8 项受管 Hook 测试通过。
- [x] 隔离 HOME、本地 Gateway fixture 和真实 Codex doctor 验收通过。
- [x] 格式、lint、git diff --check 与完整 npm run verify 通过。
- [x] 独立 Standards/Spec 审查无 P0/P1；P2 已修复或明确接受。
- [x] 创建原子本地提交并执行提交后回归。

## 下一步

M4.6 统一模型/出站门禁、版本化授权和发送字段披露；随后才为生产 Hook 接入本地 capture sink 与产品化采集策略。
