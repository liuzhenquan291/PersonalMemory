# M1.5 开发环境联合启动

- 状态：completed
- 日期：2026-08-10
- 基线：`e8080ba`

## 交付范围

- 根目录增加 `npm run dev`，联合启动 PersonalMemory Gateway 与 Vite Web。
- Gateway 增加正式 CLI 入口，复用 M1.2 配置、私有数据目录和 M1.3 Server/上游边界。
- 启动前检查端口，启动后等待 Gateway health 与 Web 首页；成功时显示两个入口和临时数据路径。
- 使用独立 POSIX 进程组管理子进程；正常信号、启动失败或任一子进程异常退出时清理完整进程树。
- 每轮在可信仓库根下创建 `0700` 隔离临时数据目录，只清理受控命名前缀；不触碰正式用户数据。
- 支持显式开发端口覆盖，Web 代理同步指向对应 PersonalMemory Gateway。

## 检查与审查门

- [x] 12 项专项测试通过：端口预占/竞态、端口值、可信临时目录、启动取消、spawn/清理失败、进程组后代、连续两轮启停、异常退出和 CLI 双信号。
- [x] 真实服务测试覆盖 Gateway/Web 健康、Vite 到 Gateway 状态代理、退出后端口释放和数据清理。
- [x] 用户实际命令在 macOS arm64 使用替代端口启动成功，Ctrl+C 后两个服务和临时数据均清理。
- [x] 默认端口已占用时立即返回 `EADDRINUSE`，未终止或复用用户已有 Vite 进程。
- [x] 异常子进程退出测试通过。
- [x] 仓库完整 verify、Python SDK 34 项测试与两处 npm 官方安全审计通过。
- [x] 独立审查 P0/P1/P2 清零。

平台状态：macOS arm64 已实测；Linux 实机待测，不作为当前已验证承诺。

最终独立复审结论：P0=0、P1=0、P2=0。
