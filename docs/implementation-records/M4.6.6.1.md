# M4.6.6.1 冻结保留期执行与恢复防复活契约

状态：completed

## 范围

- 冻结 M4.6.5 策略配置与物理删除授权的分离边界；既有 retention days 不自动获得删除授权。
- 冻结独立 L0/L1 截止点、候选批次、级联删除顺序、受管副本整件删除和失败保留事实源规则。
- 冻结受管 worker 调度、持久 lease/脱敏状态、生命周期互斥和 doctor 可观测性。
- 冻结备份前清理与恢复 staging 离线清理门禁；未实现前不启用生产自动清理。
- 非目标：本步骤不新增 schema、API、worker 调用或物理删除代码。

## 验证证据

- [x] 可实现性核验覆盖：`PrivacyDeletionService` 的 tombstone/派生/JSONL/登记副本/产品元数据/索引顺序，`/v2/atomic/delete` 与 `/v2/conversation/delete` 上游接口，`LocalMemoryCleaner` 的自然日截止点和批次保护，`restorePortableBackup` 与 `personalmemory-lifecycle-runtime.mjs` 的停服 staging/回滚入口。确认实现缺口为 retention 专用 L1 级联、独立执行授权/lease、生命周期互斥、外置策略与登记副本封套、备份 drained preflight 和离线 staging 清理。
- [x] 明确旧策略不构成破坏性授权，避免把 M4.6.5 的配置写入解释成删除同意。
- [x] 明确恢复期间服务保持停止，避免“恢复后再异步删”产生可召回窗口。
- [x] 首轮 Spec 审查发现并修复 L1 到期误删未到期来源 L0 的 P0；首轮 Standards/Spec 还修复 drained 证明、未启用备份兼容、恢复授权失效和 tombstone 后重试事实源边界。
- [x] 完整 `npm run verify` 通过，包括格式、lint、边界、构建、类型、全量测试、Golden E2E 和 SDK 测试。
- [x] Standards 与 Spec 独立复审均为 no findings；首轮 P0/P1/P2/P3 已全部修复并复核。
- [x] 原子本地提交 `b211ea1`；提交后 `git show --check`、契约/实施记录存在性及正式规则/计划关键引用检查通过。

## 下一步

M4.6.6.2 按 [保留期执行契约](../architecture/RETENTION_EXECUTION.md) 实现版本化执行授权、持久运行账本、受管 worker、L1 级联复用、独立 L0 删除、备份/恢复门禁和脱敏 doctor 状态。
