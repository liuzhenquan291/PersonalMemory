# M0.1 建立安全迁移点

- 状态：completed
- 日期：2026-08-06
- 范围：记录迁移前引用，建立本地备份分支和集成分支
- 非目标：不添加或获取上游，不合并代码，不移动 `main`，不推送远端

## 引用证据

| 引用 | SHA |
|---|---|
| `main` | `a75b9f04f099c5850eb6500dae245fa63adc2064` |
| `backup/pre-upstream-v1.0.1` | `a75b9f04f099c5850eb6500dae245fa63adc2064` |
| `integration/upstream-v1.0.1` | `a75b9f04f099c5850eb6500dae245fa63adc2064` |
| `origin/main` | `44506b61d8227332913711c2234f5cdc0c17e117` |

## 检查结果

- 当前分支为 `integration/upstream-v1.0.1`。
- 工作区和暂存区干净。
- 远程仓库只有 `origin`；没有项目级 `upstream`、上游 tracking ref 或上游 tag。
- 提交图仍是 PersonalMemory 单线历史，未发生 merge、rebase、orphan、force push 或历史改写。
- 恢复不依赖未提交文件：切换到 `backup/pre-upstream-v1.0.1` 即可回到迁移前状态。

## 独立审查

- 结论：无 P0、P1、P2，可以进入 M0.2。
- 已知限制：本地 refs/reflog 只能证明本地操作状态，不能绝对证明远端历史从未被其他主体改写；当前没有异常证据。

## 提交

本步骤只有 Git refs 变化，无文件提交；证据随 M0.2 基线报告一并提交。
