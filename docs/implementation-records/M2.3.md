# M2.3 统一查询与预算召回

- 状态：completed
- 日期：2026-08-10
- 基线：`e9aae78`

## 交付范围

- 新增 `/api/v1/recall/query`，统一读取 L0/L1/L2/L3。
- 在上游调用前按 levels 过滤，并限制 v2 只读路径。
- 强制总条数、字符、估算 token 和统一截止时间预算。
- 提供稳定层级/层内排序、截断标记和单层超时降级结果。
- 使用 `m2.1-v1` 的 100 个合成样本建立确定性词面 smoke 指标；明确不冒充端到端产品性能。

## 非目标

- 不在本步骤实现 M2.4 的列表、分页和来源下钻界面。
- 不暴露尚未在上游真正召回前执行的 type/time/session 过滤。
- 不修改 L2/L3，也不开放上游 v2 写入、删除和 instance/destroy。

## 检查与审查门

- [x] Gateway 类型、42 项专项测试和完整 `npm run verify` 通过。
- [x] levels 预过滤、稳定排序、无结果、格式错误、两类超时降级及条数/字符/token 预算边界通过。
- [x] 关键词、语义、混合检索三种上游模式专项测试通过。
- [x] 100 样本 smoke 记录 Recall@5=1、无依据注入率=0、过期命中率=0、无答案拒答率=1、P95 < 10 ms。
- [x] Python SDK 34 项测试通过，两处 npm 官方安全审计均为 0 漏洞。
- [x] `git diff --check`、敏感信息扫描和独立审查 P0/P1/P2 清零。

## 审查修正

- 原字符/token 合并估算会高估非 ASCII 内容可用预算；改为 ASCII 4 字符/token、非 ASCII 2 token/字符的保守独立预算，并按 Unicode 码点截断。
- 上游客户端自身较短超时原会被归类为不可用；现统一归入 `TIMEOUT`，同时保留其他成功层结果。
- 原评测结果只作为确定性编排 smoke 基线。M2 退出收口新增
  `src/gateway/sqlite-recall-evaluation.test.ts`：100 个版本化样本逐一通过真实 SQLite FTS5 与
  sqlite-vec 双路召回，答案样本 Recall@5、来源 ID 匹配率和无答案拒答率均为 100%，P95 门槛
  固定为 100 ms；测试使用确定性本地 embedding，不依赖网络或外部模型，可独立重现。
