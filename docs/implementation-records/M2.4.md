# M2.4 记忆列表、搜索和详情

- 状态：completed
- 日期：2026-08-11
- 基线：`d84069e`

## 交付范围

- 新增只读 `/api/v1/memories`，支持 L0/L1/L2/L3 单层浏览、搜索和分页。
- 新增 Web 层级切换、搜索、加载/空/错误状态、分页、长文本详情和来源状态。
- L0 明确标注为对话原文；缺少真实引用的 L1/L2/L3 明确标注来源未记录。
- 保持上游读取路由白名单和浏览器会话鉴权，不新增写入或删除能力。

## 已识别边界

- SQLite 当前未持久化 L1 的 `source_message_ids`，L2/L3 也没有可验证下级引用；精确来源链尚未实现。
- 搜索分页最多覆盖上游前 50 个命中，响应不声明完整总数。
- 本步骤不实现 M2.5 的修改、失效和删除操作。

## 检查与审查门

- [x] Gateway 47 项、Web 7 项专项测试和完整 `npm run verify` 通过。
- [x] 空、错误、加载、分页边界、长文本和来源语义测试通过。
- [x] Playwright 键盘主路径、320px 窄屏和 reduced-motion 共 3 项通过。
- [x] 桌面列表、详情遮罩和 320px 实际截图检查通过，无横向溢出或业务请求错误。
- [x] Python SDK 34 项测试通过，两处 npm 官方安全审计均为 0 漏洞。
- [x] `git diff --check`、敏感信息扫描和审查 P0/P1/P2 清零。

## 审查修正

- 搜索结果受上游 50 条候选限制时不返回伪造总数，`total` 明确为 `null`。
- L3 搜索无命中时总数与筛选结果保持一致，不显示误导性的 1 条记录。
- 详情对话框打开后移动并约束焦点，Escape 关闭后将焦点还给来源卡片。
- Playwright 支持显式复用已启动的隔离服务，并把冷启动等待上限提高到 10 分钟；默认仍拒绝复用未知服务。
